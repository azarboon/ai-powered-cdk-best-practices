# =============================================================================
# GitHub Monitor CDK Application - Environment Variables Template
# =============================================================================
# 
# INSTRUCTIONS:
##1. Fill in the actual values for your deployment
# 2. Source the .env file before running CDK commands: source .env
# 4. NEVER commit sensitive information to git
#
# =============================================================================


# AWS Account ID - Your target AWS account for deployment
CDK_DEFAULT_ACCOUNT=339713047495
# @azarboon: rename it to AWS_DEFAULT_ACCOUNT

# AWS Region - Target region for deployment (us-east-1 recommended for GitHub webhooks)
CDK_DEFAULT_REGION=us-east-1
# @azarboon: rename it to AWS_DEFAULT_REGION

# This secret must also be configured in your GitHub webhook settings to enable authentication.
GITHUB_WEBHOOK_SECRET=my-webhook-secret
CDK_STACK_NAME=GitHubMonitorStack

# Specify the target repository to monitor in the format owner/repo. Ensure there are no leading or trailing slashes.
GITHUB_REPOSITORY=azarboon/dummy


# GitHub API Base URL - Usually no need to change unless using GitHub Enterprise
GITHUB_API_BASE=https://api.github.com

# Email address for SNS notifications
NOTIFICATION_EMAIL=mipry99qv9@jxpomup.com

# =============================================================================
# DEPLOYMENT CONFIGURATION (OPTIONAL)
# =============================================================================

# Environment tag for resource tagging and cost tracking

ENVIRONMENT=dev
