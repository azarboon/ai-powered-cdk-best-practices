#!/bin/bash
. "$(dirname -- "$0")/_/husky.sh"

# Source the user's bashrc to get full environment
if [ -f "$HOME/.bashrc" ]; then
    source "$HOME/.bashrc"
fi

# Change to project directory
cd "$(dirname "$0")/.."

# Run with npm since it should now be available
npm run lint-staged-files
