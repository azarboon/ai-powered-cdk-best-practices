#!/bin/bash
. "$(dirname -- "$0")/_/husky.sh"

# Change to project directory
cd "$(dirname "$0")/.."

echo "$ npm run lint:fix"
npm run lint:fix

echo "$ npm run validate"
npm run validate

echo "✅ Pre-commit validations completed successfully"
