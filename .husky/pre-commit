#!/bin/bash
. "$(dirname -- "$0")/_/husky.sh"

# Change to project directory
cd "$(dirname "$0")/.."

echo "$ npm run lint:fix"
npm run lint:fix

# @mahdi: you may wanna add npm run validate in here

echo "✅ Pre-commit validations completed successfully"
