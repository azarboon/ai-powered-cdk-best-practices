#!/bin/bash
. "$(dirname -- "$0")/_/husky.sh"

# Change to project directory
cd "$(dirname "$0")/.."

echo "🔍 Running ESLint validation with debug output..."

npm run lint:fix

echo "✅ ESLint validation completed successfully"
